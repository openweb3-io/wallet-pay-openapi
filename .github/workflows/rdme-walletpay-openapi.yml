# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-07-16T12:55:33.633Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: Sync WalletPay OpenAPI definition to ReadMe 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi openapi-walletpay.json --key=${{ secrets.RDME_WALLET_PAY_OPENAPI_KEY }} --id=${{ secrets.RDME_WALLET_PAY_OPENAPI_ID }}
